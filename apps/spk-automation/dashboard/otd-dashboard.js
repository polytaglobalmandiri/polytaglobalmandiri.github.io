(function(){
  'use strict';
  var state={rows:[],filtered:[],meta:null};
  var els={status:document.getElementById('dataStatus'),spk:document.getElementById('kpiSpk'),kg:document.getElementById('kpiKg'),pcs:document.getElementById('kpiPcs'),aging:document.getElementById('kpiAging'),oldest:document.getElementById('kpiOldest'),processBars:document.getElementById('processBars'),marketingBars:document.getElementById('marketingBars'),rows:document.getElementById('otdRows'),caption:document.getElementById('resultCaption'),snapshot:document.getElementById('snapshotLabel'),search:document.getElementById('searchInput'),uom:document.getElementById('uomFilter'),process:document.getElementById('processFilter'),agingFilter:document.getElementById('agingFilter'),empty:document.getElementById('emptyState'),recapYear:document.getElementById('recapYear'),recapTotals:document.getElementById('recapTotals'),routingRecap:document.getElementById('routingRecap'),materialRecap:document.getElementById('materialRecap'),routeMaterialRecap:document.getElementById('routeMaterialRecap'),monthlyRecap:document.getElementById('monthlyRecap'),marketingRecap:document.getElementById('marketingRecap')};
  function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];});}
  function num(v,decimals){return Number(v||0).toLocaleString('id-ID',{minimumFractionDigits:decimals||0,maximumFractionDigits:decimals==null?1:decimals});}
  function dateLabel(v){if(!v)return '—';var d=new Date(v+'T00:00:00');return isNaN(d)?v:d.toLocaleDateString('id-ID',{day:'2-digit',month:'short',year:'numeric'});}
  function group(rows,key){return rows.reduce(function(out,row){var name=row[key]||'Belum ditentukan';out[name]=(out[name]||0)+1;return out;},{});}
  function bars(target,values){var entries=Object.keys(values).map(function(k){return[k,values[k]];}).sort(function(a,b){return b[1]-a[1];});var max=Math.max.apply(null,entries.map(function(x){return x[1];}).concat([1]));target.innerHTML=entries.map(function(item){return '<div class="bar-item"><span class="bar-label" title="'+esc(item[0])+'">'+esc(item[0])+'</span><span class="bar-track"><span class="bar-fill" style="width:'+Math.max(8,item[1]/max*100)+'%"></span></span><strong class="bar-value">'+item[1]+'</strong></div>';}).join('');}
  function renderKpis(){var outstanding=state.rows.filter(function(r){return Number(r.outstanding)>0;});var kg=outstanding.filter(function(r){return r.uom==='KG';}).reduce(function(n,r){return n+Number(r.outstanding);},0);var pcs=outstanding.filter(function(r){return r.uom==='PCS';}).reduce(function(n,r){return n+Number(r.outstanding);},0);var old=Math.max.apply(null,state.rows.map(function(r){return Number(r.aging)||0;}).concat([0]));els.spk.textContent=outstanding.length+' SPK';els.kg.textContent=num(kg,1)+' KG';els.pcs.textContent=num(pcs,0)+' PCS';els.aging.textContent=state.rows.filter(function(r){return Number(r.aging)>=30;}).length+' SPK';els.oldest.textContent='aging tertinggi '+old+' hari';bars(els.processBars,group(outstanding,'proses'));bars(els.marketingBars,group(outstanding,'marketing'));}
  function addQty(target,row){target[row.uom]=(target[row.uom]||0)+Number(row.order||0);}
  function qtyLabel(q){var labels=[];if(q.KG)labels.push(num(q.KG,1)+' KG');if(q.PCS)labels.push(num(q.PCS,0)+' PCS');return labels.join(' · ')||'0';}
  function yearOf(row){return Number(String(row.tanggalPo||'').slice(0,4))||0;}
  function renderStatList(target,entries,kind){var max=Math.max.apply(null,entries.map(function(x){return x.count;}).concat([1]));target.innerHTML='<div class="recap-stat-list">'+entries.map(function(item,index){return '<div class="recap-stat-row" style="animation-delay:'+(index*.045)+'s"><div class="recap-stat-copy"><strong title="'+esc(item.name)+'">'+esc(item.name)+'</strong><small>'+item.count+' SPK</small></div><span class="recap-meter"><span style="width:'+Math.max(7,item.count/max*100)+'%"></span></span><span class="recap-qty">'+qtyLabel(item.qty)+'</span></div>';}).join('')+'</div>'+(entries.length?'':'<div class="recap-empty">Belum ada data '+esc(kind)+' untuk tahun ini.</div>');}
  function renderRecap(){var year=Number(els.recapYear.value),rows=state.rows.filter(function(r){return yearOf(r)===year;}),routing={},materials={},routeMaterials={},marketing={},months=Array.from({length:12},function(){return{};});rows.forEach(function(row){var material=row.material||'Belum ditentukan',m=Number(String(row.tanggalPo).slice(5,7))-1;materials[material]=materials[material]||{name:material,count:0,qty:{}};materials[material].count++;addQty(materials[material].qty,row);marketing[row.marketing||'Belum ditentukan']=(marketing[row.marketing||'Belum ditentukan']||0)+1;if(m>=0&&m<12)months[m][material]=(months[m][material]||0)+1;Array.from(new Set((row.routing||[]).filter(Boolean))).forEach(function(route){routing[route]=routing[route]||{name:route,count:0,qty:{}};routing[route].count++;addQty(routing[route].qty,row);routeMaterials[route]=routeMaterials[route]||{};routeMaterials[route][material]=routeMaterials[route][material]||{count:0,qty:{}};routeMaterials[route][material].count++;addQty(routeMaterials[route][material].qty,row);});});var routeEntries=Object.keys(routing).map(function(k){return routing[k];}).sort(function(a,b){return b.count-a.count||a.name.localeCompare(b.name);}),materialEntries=Object.keys(materials).map(function(k){return materials[k];}).sort(function(a,b){return b.count-a.count||a.name.localeCompare(b.name);}),marketingEntries=Object.keys(marketing).map(function(k){return{name:k,count:marketing[k]};}).sort(function(a,b){return b.count-a.count||a.name.localeCompare(b.name);}),totalQty={};rows.forEach(function(r){addQty(totalQty,r);});els.recapTotals.innerHTML=[['Total SPK',rows.length+' SPK','accent'],['Total Routing',routeEntries.length+' Proses',''],['Total Bahan',materialEntries.length+' Jenis',''],['Total Marketing',marketingEntries.length+' Orang',''],['Qty KG',num(totalQty.KG||0,1)+' KG',''],['Qty PCS',num(totalQty.PCS||0,0)+' PCS','']].map(function(x){return'<article class="recap-total '+x[2]+'"><small>'+x[0]+'</small><strong>'+x[1]+'</strong></article>';}).join('');renderStatList(els.routingRecap,routeEntries,'routing');renderStatList(els.materialRecap,materialEntries,'bahan');els.routeMaterialRecap.innerHTML=routeEntries.map(function(route,index){var items=Object.keys(routeMaterials[route.name]).map(function(name){return{name:name,data:routeMaterials[route.name][name]};}).sort(function(a,b){return b.data.count-a.data.count;});return'<article class="route-material-card" style="animation-delay:'+(index*.05)+'s"><h4>'+esc(route.name)+'<span>'+route.count+' SPK</span></h4>'+items.map(function(item){return'<div class="route-material-item"><span>'+esc(item.name)+'<small> · '+item.data.count+' SPK</small></span><b>'+qtyLabel(item.data.qty)+'</b></div>';}).join('')+'</article>';}).join('')||'<div class="recap-empty">Belum ada pemetaan routing dan bahan.</div>';var monthNames=['Jan','Feb','Mar','Apr','Mei','Jun','Jul','Agu','Sep','Okt','Nov','Des'];els.monthlyRecap.innerHTML='<table class="monthly-table"><thead><tr><th>Bahan</th>'+monthNames.map(function(m){return'<th>'+m+'</th>';}).join('')+'<th>Total</th></tr></thead><tbody>'+materialEntries.map(function(mat){var total=0,cells=months.map(function(month){var value=month[mat.name]||0;total+=value;return'<td class="'+(value?'has-data':'')+'">'+(value||'—')+'</td>';}).join('');return'<tr><td>'+esc(mat.name)+'</td>'+cells+'<td><strong>'+total+'</strong></td></tr>';}).join('')+'</tbody><tfoot><tr><td>Total SPK</td>'+months.map(function(month){var total=Object.keys(month).reduce(function(n,k){return n+month[k];},0);return'<td>'+total+'</td>';}).join('')+'<td>'+rows.length+'</td></tr></tfoot></table>';els.marketingRecap.innerHTML=marketingEntries.map(function(item,index){return'<article class="rank-card '+(index===0?'champion':'')+'" style="animation-delay:'+(index*.06)+'s"><div class="rank-place"><span>Peringkat '+(index+1)+'</span>'+(index===0?'<i class="fa-solid fa-crown" title="Raja/Ratu SPK"></i>':'')+'</div><h4>'+esc(item.name)+'</h4><strong>'+item.count+'</strong><small>SPK</small>'+(index===0?'<div class="champion-label">Raja/Ratu SPK</div>':'')+'</article>';}).join('')||'<div class="recap-empty">Belum ada data marketing.</div>';}
  function renderMarketingContributionList(){var year=Number(els.recapYear.value),rows=state.rows.filter(function(r){return yearOf(r)===year;}),groups={},totalKg=0,totalPcs=0;rows.forEach(function(row){var name=row.marketing||'Belum ditentukan';groups[name]=groups[name]||{name:name,count:0,kg:0,pcs:0};groups[name].count++;if(row.uom==='KG'){groups[name].kg+=Number(row.order||0);totalKg+=Number(row.order||0);}else if(row.uom==='PCS'){groups[name].pcs+=Number(row.order||0);totalPcs+=Number(row.order||0);}});var entries=Object.keys(groups).map(function(k){return groups[k];}).sort(function(a,b){return b.count-a.count||(b.kg+b.pcs)-(a.kg+a.pcs)||a.name.localeCompare(b.name);});var head='<div class="marketing-list-head"><span>Peringkat</span><span>Marketing</span><span>SPK</span><span>Kontribusi KG</span><span>Kontribusi PCS</span></div>';els.marketingRecap.innerHTML=head+entries.map(function(item,index){var kgPct=totalKg?item.kg/totalKg*100:0,pcsPct=totalPcs?item.pcs/totalPcs*100:0;return '<article class="marketing-list-row '+(index===0?'champion':'')+'" style="animation-delay:'+(index*.05)+'s"><span class="marketing-rank">'+(index+1)+'</span><div class="marketing-person"><strong>'+(index===0?'<i class="fa-solid fa-crown"></i>':'')+esc(item.name)+'</strong><small>'+(index===0?'Raja/Ratu SPK tahun '+year:'Kontributor produksi')+'</small></div><div class="marketing-spk">'+item.count+'<small>SPK</small></div><div class="marketing-contribution kg"><div class="marketing-contribution-head"><span>KG</span><b>'+num(item.kg,1)+' · '+num(kgPct,1)+'%</b></div><div class="contribution-track"><span style="width:'+kgPct+'%"></span></div></div><div class="marketing-contribution pcs"><div class="marketing-contribution-head"><span>PCS</span><b>'+num(item.pcs,0)+' · '+num(pcsPct,1)+'%</b></div><div class="contribution-track"><span style="width:'+pcsPct+'%"></span></div></div></article>';}).join('');}
  var renderRecapBase=renderRecap;
  renderRecap=function(){renderRecapBase();renderMarketingContributionList();};
  function renderTable(){els.rows.innerHTML=state.filtered.map(function(r){var progress=Math.max(0,Math.min(100,Number(r.hasil)/Math.max(1,Number(r.order))*100));var positive=Number(r.outstanding)>0;var critical=Number(r.aging)>=45;return '<tr><td><strong>'+esc(r.spk)+'</strong><small>'+esc(r.customer)+'</small></td><td><strong>'+esc(r.brand)+'</strong><small>'+esc(r.material)+' · '+esc(r.marketing)+'</small></td><td class="number"><strong>'+num(r.order,r.uom==='KG'?1:0)+' '+esc(r.uom)+'</strong></td><td class="number"><strong>'+num(r.hasil,r.uom==='KG'?1:0)+'</strong><span class="progress-track"><span class="progress-fill" style="width:'+progress+'%"></span></span></td><td class="number '+(positive?'outstanding-positive':'outstanding-complete')+'">'+(positive?num(r.outstanding,r.uom==='KG'?1:0)+' '+esc(r.uom):'Selesai')+'</td><td><span class="process-badge">'+esc(r.proses||'—')+'</span><small>'+esc((r.routing||[]).join(' → '))+'</small></td><td class="number"><span class="aging-badge '+(critical?'critical':'')+'">'+num(r.aging,0)+' hari</span></td><td><strong>'+dateLabel(r.targetKirim)+'</strong><small>PO '+dateLabel(r.tanggalPo)+'</small></td></tr>';}).join('');els.empty.hidden=state.filtered.length>0;els.caption.textContent=state.filtered.length+' dari '+state.rows.length+' SPK ditampilkan · KG dan PCS dihitung terpisah';}
  function applyFilters(){var q=els.search.value.trim().toLowerCase(),uom=els.uom.value,process=els.process.value,minAging=Number(els.agingFilter.value)||0;state.filtered=state.rows.filter(function(r){var hay=[r.spk,r.customer,r.brand,r.material,r.marketing].join(' ').toLowerCase();return(!q||hay.indexOf(q)>-1)&&(!uom||r.uom===uom)&&(!process||r.proses===process)&&Number(r.aging)>=minAging;}).sort(function(a,b){return Number(b.aging)-Number(a.aging);});renderTable();}
  function load(){fetch('/assets/data/otd-dashboard.json?v=20260817-1',{cache:'no-store'}).then(function(r){if(!r.ok)throw new Error('Data OTD tidak dapat dimuat.');return r.json();}).then(function(data){state.meta=data;state.rows=Array.isArray(data.rows)?data.rows:[];var processes=Array.from(new Set(state.rows.map(function(r){return r.proses;}).filter(Boolean))).sort(),years=Array.from(new Set(state.rows.map(yearOf).filter(Boolean))).sort(function(a,b){return b-a;});els.process.innerHTML='<option value="">Semua Proses</option>'+processes.map(function(p){return'<option>'+esc(p)+'</option>';}).join('');els.recapYear.innerHTML=years.map(function(y){return'<option value="'+y+'">'+y+'</option>';}).join('');els.snapshot.textContent='Snapshot '+new Date(data.snapshotAt).toLocaleString('id-ID',{dateStyle:'medium',timeStyle:'short'});els.status.innerHTML='<i class="fa-solid fa-circle-check"></i> Data OTD tersedia';renderKpis();renderRecap();applyFilters();}).catch(function(err){els.status.innerHTML='<i class="fa-solid fa-triangle-exclamation"></i> OTD gagal dimuat';els.status.classList.add('is-error');els.caption.textContent=err.message;els.empty.hidden=false;});}
  [els.search,els.uom,els.process,els.agingFilter].forEach(function(el){el.addEventListener(el===els.search?'input':'change',applyFilters);});
  els.recapYear.addEventListener('change',renderRecap);
  load();
})();
